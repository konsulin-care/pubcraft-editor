# Pubcraft Editor Environment Configuration Template
# This file provides a template for configuring your Pubcraft Editor deployment
# Copy this file to .env and fill in the values

# GitHub OAuth Configuration (REQUIRED)
# Obtain these from GitHub Developer Settings
VITE_GITHUB_CLIENT_ID=your_github_client_id
VITE_GITHUB_REDIRECT_URI=http://localhost:5173/github/callback

# ORCID OAuth Configuration (REQUIRED)
# Obtain these from ORCID Developer Tools
VITE_ORCID_CLIENT_ID=your_orcid_client_id
VITE_ORCID_REDIRECT_URI=http://localhost:5173/orcid/callback

# Optional Application Configuration
# Customize these to match your deployment
VITE_APP_NAME=Pubcraft Editor
VITE_APP_VERSION=0.1.0

# Debug Mode (Enables additional logging and warnings)
# Set to 'true' during development, 'false' in production
VITE_DEBUG_MODE=false

# API Base URLs (Optional, can use defaults)
VITE_GITHUB_API_BASE_URL=https://api.github.com
VITE_ORCID_API_BASE_URL=https://orcid.org/oauth

# DEPLOYMENT INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace placeholder values with your actual configuration
# 3. Ensure .env is NOT committed to version control
# 4. Use environment variables or docker secrets in production
